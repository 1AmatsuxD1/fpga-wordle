# 🔌 คู่มือการเชื่อมต่อ FPGA #1 และ FPGA #2

## 📋 **สรุปการเชื่อมต่อ:**

ต้องต่อสาย **11 เส้น + GND** ระหว่าง FPGA #2 และ FPGA #1

---

## 🔗 **ตารางการเชื่อมต่อ:**

### **A. Serial Communication - ส่งคำทาย (FPGA #2 → FPGA #1)**

| สัญญาณ | FPGA #2 | Connector | ← → | FPGA #1 | Connector | คำอธิบาย |
|--------|---------|-----------|-----|---------|-----------|----------|
| **TX Data** | P5 | K1-15 | → | P5 | K1-15 | ข้อมูล 40-bit (5 ตัวอักษร) |
| **TX Clock** | P7 | K1-13 | → | P7 | K1-13 | Clock 2.5 MHz (synchronous) |
| **Data Valid** | P9 | K1-11 | → | P9 | K1-11 | FPGA #2 ส่งข้อมูลเสร็จแล้ว |

### **B. Serial Communication - รับผลลัพธ์ (FPGA #1 → FPGA #2)**

| สัญญาณ | FPGA #1 | Connector | ← → | FPGA #2 | Connector | คำอธิบาย |
|--------|---------|-----------|-----|---------|-----------|----------|
| **TX Data** | P11 | K1-9 | → | P11 | K1-9 | ผลลัพธ์ 15-bit (5×3 สี) |
| **TX Clock** | P14 | K1-7 | → | P14 | K1-7 | Clock 2.5 MHz (synchronous) |
| **Acknowledge** | P16 | K1-5 | → | P16 | K1-5 | FPGA #1 รับข้อมูลแล้ว |
| **Result Valid** | P21 | K1-3 | → | P21 | K1-3 | FPGA #1 ส่งผลลัพธ์แล้ว |

### **C. Game Status (FPGA #1 → FPGA #2)**

| สัญญาณ | FPGA #1 | Connector | ← → | FPGA #2 | Connector | คำอธิบาย |
|--------|---------|-----------|-----|---------|-----------|----------|
| **Status[0]** | P6 | K2-15 | → | P6 | K2-15 | Bit 0 (LSB) |
| **Status[1]** | P8 | K2-13 | → | P8 | K2-13 | Bit 1 |
| **Status[2]** | P10 | K2-11 | → | P10 | K2-11 | Bit 2 (MSB) |

**Game Status Codes:**
- `000` = กำลังเล่น (PLAYING)
- `001` = ชนะ (WIN)
- `010` = แพ้ (LOSE)

### **D. Ground (GND) - สำคัญมาก! ⚠️**

| FPGA #2 | ← → | FPGA #1 | คำอธิบาย |
|---------|-----|---------|----------|
| **GND** | ← → | **GND** | **ต้องต่อ!** (reference voltage) |

---

## 🎯 **ขั้นตอนการต่อสาย:**

### **วิธีที่ 1: ใช้ Ribbon Cable (แนะนำ)** ⭐

```
1. ใช้ Ribbon Cable 20 pins
2. ต่อ K1 ของ FPGA #2 → K1 ของ FPGA #1
3. ต่อ K2 ของ FPGA #2 → K2 ของ FPGA #1
4. ⚠️ ระวัง! Pin 1 ต้องตรง Pin 1 (ดูที่จุดสีแดงบน ribbon)
5. ต่อ GND ระหว่างบอร์ด (ใช้สายจัมเปอร์)
```

### **วิธีที่ 2: ใช้ Jumper Wires**

```
ใช้สายจัมเปอร์ 12 เส้น (11 สาย + GND):

📌 K1 Connector (7 เส้น) - ต่อตรงกัน (P-to-P):
- FPGA #2 P5  → FPGA #1 P5  (TX Data จาก #2)
- FPGA #2 P7  → FPGA #1 P7  (TX Clock จาก #2)
- FPGA #2 P9  → FPGA #1 P9  (Data Valid จาก #2)
- FPGA #2 P11 → FPGA #1 P11 (TX Data จาก #1)
- FPGA #2 P14 → FPGA #1 P14 (TX Clock จาก #1)
- FPGA #2 P16 → FPGA #1 P16 (Acknowledge จาก #1)
- FPGA #2 P21 → FPGA #1 P21 (Result Valid จาก #1)

📌 K2 Connector (3 เส้น):
- FPGA #2 P6  → FPGA #1 P6  (Status[0])
- FPGA #2 P8  → FPGA #1 P8  (Status[1])
- FPGA #2 P10 → FPGA #1 P10 (Status[2])

📌 Ground (1 เส้น - สำคัญ!):
- FPGA #2 GND → FPGA #1 GND
```

---

## ✅ **Checklist ก่อนเปิดเครื่อง:**

### **1. ตรวจสอบการต่อสาย:**
```
☐ P5:  FPGA #2 P5  ↔ FPGA #1 P5  (TX Data #2 / RX Data #1)
☐ P7:  FPGA #2 P7  ↔ FPGA #1 P7  (TX Clock #2 / RX Clock #1)
☐ P9:  FPGA #2 P9  ↔ FPGA #1 P9  (Data Valid #2 / RX Valid #1)
☐ P11: FPGA #2 P11 ↔ FPGA #1 P11 (RX Data #2 / TX Data #1)
☐ P14: FPGA #2 P14 ↔ FPGA #1 P14 (RX Clock #2 / TX Clock #1)
☐ P16: FPGA #2 P16 ↔ FPGA #1 P16 (RX Ack #2 / TX Ack #1)
☐ P21: FPGA #2 P21 ↔ FPGA #1 P21 (RX Valid #2 / TX Valid #1)
☐ P6:  FPGA #2 P6  ↔ FPGA #1 P6  (Status[0])
☐ P8:  FPGA #2 P8  ↔ FPGA #1 P8  (Status[1])
☐ P10: FPGA #2 P10 ↔ FPGA #1 P10 (Status[2])
☐ GND: FPGA #2 GND ↔ FPGA #1 GND ⚠️ สำคัญ!
```

### **2. ใช้ Multimeter ตรวจสอบ Continuity:**
```
☐ ทุกสายต้อง beep (มีการต่อผ่าน)
☐ ไม่มี short circuit (สายติดกัน)
☐ GND ต่อถูกต้อง
```

### **3. Upload โปรแกรม:**
```
☐ FPGA #1: fpga1_top_serial.bit
☐ FPGA #2: fpga2_top_3bit_rgb_Version2.bit (TEST_MODE = false)
```

---

## 🎮 **การทดสอบหลังเชื่อมต่อ:**

### **Test 1: ตรวจสอบ LED**

**FPGA #2:**
```
L0: ON (DCM locked)
L1-L3: แสดง buffer_index (000 → 001 → ... → 101)
```

**FPGA #1:**
```
L3: กระพริบ (Heartbeat - 0.5s interval)
L0: ติดเมื่อ FPGA #2 ส่งข้อมูล (data_valid)
L1: ติดเมื่อรับคำสำเร็จ (word_received)
L2: กระพริบเมื่อรับ serial clock
```

### **Test 2: ทดสอบการเล่น**

```
1. พิมพ์คำ 5 ตัว (เช่น HELLO)
2. กด Enter
3. ดู LED FPGA #1:
   - L0 ต้องติด (data_valid)
   - L1 ต้องติด (word_received)
4. รอ 1-2 วินาที
5. จอต้องแสดงสี:
   - เขียว = ตัวอักษรถูกต้อง ตำแหน่งถูกต้อง
   - เหลือง = ตัวอักษรถูก แต่ตำแหน่งผิด
   - ม่วง/เทา = ไม่มีตัวอักษรนี้
```

### **Test 3: ทดสอบชนะ/แพ้**

```
ชนะ:
- พิมพ์คำตอบที่ถูกต้อง
- จอแสดงสีเขียวทั้ง 5 ตัว
- Status Bar (Y>450) แสดงสีเขียว

แพ้:
- ทาย 6 ครั้งแล้วยังไม่ถูก
- Status Bar แสดงสีเหลือง (ไม่มีแดง)
```

---

## ⚠️ **ปัญหาที่อาจพบและวิธีแก้:**

### **ปัญหา 1: จอแสดงแต่สีม่วง (ไม่มีสี)**
```
สาเหตุ: FPGA #1 ไม่ตอบกลับ

แก้ไข:
1. เช็ค LED FPGA #1: L3 ต้องกระพริบ (Heartbeat)
2. ถ้า L3 ไม่กระพริบ → FPGA #1 ไม่ทำงาน
3. ถ้า L3 กระพริบ แต่ L0-L2 ดับ → สายไม่ต่อ/ต่อผิด
4. ตรวจ GND ต้องต่อ!
```

### **ปัญหา 2: FPGA #2 ค้างที่ state WAIT_ACKNOWLEDGE**
```
สาเหตุ: FPGA #1 ไม่ส่ง acknowledge กลับ

แก้ไข:
1. เช็คสาย P9 (FPGA #1) → P16 (FPGA #2)
2. เช็ค LED FPGA #1 L0 ว่าติดหรือไม่
3. ตรวจ GND
```

### **ปัญหา 3: LED FPGA #1 L0-L2 ดับหมด**
```
สาเหตุ: ไม่ได้รับสัญญาณจาก FPGA #2

แก้ไข:
1. เช็คสาย TX: P5, P7, P9 (FPGA #2)
2. เช็คสาย RX: P11, P14, P21 (FPGA #1)
3. ใช้ multimeter วัด continuity
4. **เช็ค GND!** (สำคัญที่สุด)
```

### **ปัญหา 4: Timeout เกิดขึ้นบ่อย**
```
สาเหตุ: Serial communication ช้าหรือขาดหาย

แก้ไข:
1. เช็คสาย Clock: P7 (FPGA #2) → P14 (FPGA #1)
2. เช็คสาย Clock: P14 (FPGA #2) → P7 (FPGA #1)
3. ตรวจสอบ timing constraints ใน UCF
4. ลด length ของสาย (ไม่เกิน 30cm)
```

---

## 📊 **ข้อมูล Serial Protocol:**

### **Timing:**
```
Clock: 2.5 MHz (period = 400 ns)
Data: MSB first
Width: 
  - TX: 40 bits (5 letters × 8 bits)
  - RX: 15 bits (5 colors × 3 bits)
Transfer time:
  - TX: 40 bits ÷ 2.5 MHz = 16 μs
  - RX: 15 bits ÷ 2.5 MHz = 6 μs
```

### **Handshake:**
```
1. FPGA #2 ส่ง data + clock
2. FPGA #2 set data_valid = '1'
3. FPGA #1 รับข้อมูล
4. FPGA #1 set acknowledge = '1'
5. FPGA #2 set data_valid = '0'
6. FPGA #1 ประมวลผล
7. FPGA #1 ส่ง result + clock
8. FPGA #1 set result_valid = '1'
9. FPGA #2 รับผลลัพธ์
10. FPGA #2 แสดงสีบนจอ
```

---

## 🎯 **คำตอบในเกม (word_rom.vhd):**

```vhdl
Address 0: APPLE
Address 1: GRAPE
Address 2: LEMON
Address 3: PEACH
Address 4: MELON
Address 5: BERRY
Address 6: MANGO
Address 7: CHERRY
Address 8: PLUM
Address 9: KIWI
(ฯลฯ - ดูใน word_rom_20mhz.vhd)
```

---

## ✅ **หลังจากต่อสายเรียบร้อย:**

1. **Upload FPGA #1** (fpga1_top_serial.bit)
2. **Upload FPGA #2** (fpga2_top_3bit_rgb_Version2.bit)
3. **ตรวจ LED:**
   - FPGA #1 L3 กระพริบ ✅
   - FPGA #2 L0 ติด ✅
4. **ทดสอบ:**
   - พิมพ์ APPLE → กด Enter → ต้องเห็นสี!
5. **เล่นเกม Wordle!** 🎮

---

## 🏆 **Good Luck!**

ถ้ามีปัญหาตรงไหน กลับมาถามได้เลยครับ! 😊
