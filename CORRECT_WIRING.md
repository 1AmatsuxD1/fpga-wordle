# ✅ ตารางการเชื่อมต่อที่ถูกต้อง (ตรวจสอบแล้ว)

## 🎯 **หลักการสำคัญ:**

การเชื่อมต่อระหว่าง FPGA #1 และ FPGA #2 ใช้หลักการ **Pin-to-Pin** (ต่อตรงกัน)

- ไม่ใช่ Cross-over (ไม่สลับสาย TX-RX)
- ใช้ Ribbon Cable ต่อตรง K1-K1, K2-K2
- หรือใช้ Jumper Wires ต่อ P-to-P

---

## 📊 **ตารางการเชื่อมต่อที่ถูกต้อง:**

### **K1 Connector - Serial Communication (7 สาย)**

| Pin | FPGA #2 Signal | ← → | FPGA #1 Signal | Function |
|-----|----------------|-----|----------------|----------|
| **P5** | serial_tx_data (out) | ↔ | serial_rx_data (in) | Data: #2→#1 |
| **P7** | serial_tx_clk (out) | ↔ | serial_rx_clk (in) | Clock: #2→#1 |
| **P9** | data_valid (out) | ↔ | data_valid (in) | Valid: #2→#1 |
| **P11** | serial_rx_data (in) | ↔ | serial_tx_data (out) | Data: #1→#2 |
| **P14** | serial_rx_clk (in) | ↔ | serial_tx_clk (out) | Clock: #1→#2 |
| **P16** | acknowledge (in) | ↔ | acknowledge (out) | Ack: #1→#2 |
| **P21** | result_valid (in) | ↔ | result_valid (out) | Valid: #1→#2 |

### **K2 Connector - Game Status (3 สาย)**

| Pin | FPGA #2 Signal | ← → | FPGA #1 Signal | Function |
|-----|----------------|-----|----------------|----------|
| **P6** | game_status[0] (in) | ↔ | game_status[0] (out) | Status bit 0 |
| **P8** | game_status[1] (in) | ↔ | game_status[1] (out) | Status bit 1 |
| **P10** | game_status[2] (in) | ↔ | game_status[2] (out) | Status bit 2 |

### **Ground (1 สาย - สำคัญมาก!)**

| FPGA #2 | ← → | FPGA #1 | Function |
|---------|-----|---------|----------|
| **GND** | ↔ | **GND** | Reference voltage |

---

## 🔌 **วิธีต่อสาย:**

### **วิธีที่ 1: Ribbon Cable (แนะนำ)** ⭐

```
1. ใช้ Ribbon Cable 20-pin (2.54mm pitch)
2. ต่อ K1 (FPGA #2) → K1 (FPGA #1) ตรงๆ
3. ต่อ K2 (FPGA #2) → K2 (FPGA #1) ตรงๆ
4. ระวัง: Pin 1 ต้องตรง Pin 1 (ดูจุดสีแดง)
5. ต่อ GND แยกด้วยสายจัมเปอร์
```

**ข้อดี:**
- ✅ ง่าย รวดเร็ว
- ✅ ไม่สับสน
- ✅ ไม่ต้องนับ pin ทีละเส้น

### **วิธีที่ 2: Jumper Wires**

```
ต่อตรงกัน (Pin-to-Pin):

K1 (7 สาย):
P5  ↔ P5
P7  ↔ P7
P9  ↔ P9
P11 ↔ P11
P14 ↔ P14
P16 ↔ P16
P21 ↔ P21

K2 (3 สาย):
P6  ↔ P6
P8  ↔ P8
P10 ↔ P10

GND (1 สาย):
GND ↔ GND
```

---

## ⚠️ **ข้อผิดพลาดที่พบบ่อย:**

### **❌ ผิด: คิดว่าต้องสลับสาย (Cross-over)**
```
ผิด: P5 (#2) → P11 (#1)  ❌
ผิด: P7 (#2) → P14 (#1)  ❌
```

### **✅ ถูก: ต่อตรงกัน (Straight-through)**
```
ถูก: P5 (#2) ↔ P5 (#1)  ✅
ถูก: P7 (#2) ↔ P7 (#1)  ✅
```

**เหตุผล:**
- UCF files กำหนด pin ไว้แล้วว่า **ต่อตรงกัน**
- FPGA #2 มี TX (P5,P7,P9) และ RX (P11,P14,P16,P21)
- FPGA #1 มี RX (P5,P7,P9) และ TX (P11,P14,P16,P21)
- การต่อตรง P-to-P ทำให้ TX→RX อัตโนมัติ

---

## 🎯 **Data Flow:**

```
FPGA #2 → FPGA #1 (ส่งคำทาย):
serial_tx_data (P5)  → serial_rx_data (P5)
serial_tx_clk (P7)   → serial_rx_clk (P7)
data_valid (P9)      → data_valid (P9)

FPGA #1 → FPGA #2 (ส่งผลลัพธ์):
serial_tx_data (P11) → serial_rx_data (P11)
serial_tx_clk (P14)  → serial_rx_clk (P14)
acknowledge (P16)    → acknowledge (P16)
result_valid (P21)   → result_valid (P21)

FPGA #1 → FPGA #2 (สถานะเกม):
game_status[2:0] (P6,P8,P10) → game_status[2:0] (P6,P8,P10)
```

---

## ✅ **Checklist ก่อนเปิดเครื่อง:**

```
☐ P5  เชื่อมต่อกัน  ✓
☐ P7  เชื่อมต่อกัน  ✓
☐ P9  เชื่อมต่อกัน  ✓
☐ P11 เชื่อมต่อกัน  ✓
☐ P14 เชื่อมต่อกัน  ✓
☐ P16 เชื่อมต่อกัน  ✓
☐ P21 เชื่อมต่อกัน  ✓
☐ P6  เชื่อมต่อกัน  ✓
☐ P8  เชื่อมต่อกัน  ✓
☐ P10 เชื่อมต่อกัน  ✓
☐ GND เชื่อมต่อกัน  ✓ (สำคัญ!)
```

**ตรวจสอบด้วย Multimeter:**
```
☐ ทุก pin มี continuity (beep)
☐ ไม่มี short circuit
☐ GND ต่อถูกต้อง
```

---

## 🔍 **การตรวจสอบด้วย UCF Files:**

### **FPGA #2 (fpga2_constraints_3bit_rgb_Version2.ucf):**
```vhdl
-- TX (Output from FPGA #2)
NET "serial_tx_data" LOC = P5   -- K1-15
NET "serial_tx_clk"  LOC = P7   -- K1-13
NET "data_valid"     LOC = P9   -- K1-11

-- RX (Input to FPGA #2)
NET "serial_rx_data" LOC = P11  -- K1-9
NET "serial_rx_clk"  LOC = P14  -- K1-7
NET "acknowledge"    LOC = P16  -- K1-5
NET "result_valid"   LOC = P21  -- K1-3
```

### **FPGA #1 (fpga1_constraints_final.ucf):**
```vhdl
-- RX (Input to FPGA #1)
NET "serial_rx_data" LOC = P5   -- K1-15
NET "serial_rx_clk"  LOC = P7   -- K1-13
NET "data_valid"     LOC = P9   -- K1-11

-- TX (Output from FPGA #1)
NET "serial_tx_data" LOC = P11  -- K1-9
NET "serial_tx_clk"  LOC = P14  -- K1-7
NET "acknowledge"    LOC = P16  -- K1-5
NET "result_valid"   LOC = P21  -- K1-3
```

**สรุป:** ต่อตรงกันทุก pin! (P5↔P5, P7↔P7, ...)

---

## 🏆 **ยืนยันความถูกต้อง:**

✅ ตรวจสอบจาก UCF files แล้ว
✅ ต่อตรงกัน (Pin-to-Pin) ทุก pin
✅ ไม่ต้องสลับสาย (No cross-over)
✅ ใช้ Ribbon Cable ต่อตรงได้เลย

---

**หมายเหตุ:** เอกสารนี้ตรวจสอบความถูกต้องจาก UCF files แล้ว (ณ วันที่ 4 พ.ย. 2568)
